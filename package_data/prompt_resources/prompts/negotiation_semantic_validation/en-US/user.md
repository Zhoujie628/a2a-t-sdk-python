Declared negotiation type: [negotiation_type]
Declared template identifier: [template_uri]

Parameter schema:
[schema]

Negotiation message to validate:
[input]

Following the system prompt, perform all validation tasks and the parameter extraction on the negotiation message above. Output one JSON object containing all 4 required keys only.
