## 协商上下文
{{协商上下文}}（必填）
要求：
每次协商必须包含以下信息：
- id: 一次协商会话的唯一标识，使用UUID，协商进入终态（Accept或Reject），则此协商会话终止；举例：3dbc13b5-bd57-4c2b-b503-24e381b6c8d3
- round: 当前轮次；类型：正整数；举例：1
- maxRounds: 最大轮次，超过此轮次，则结束协商；类型：正整数；举例：5

## 信息协商
请根据<所需信息项>补充相关内容。

## 所需信息项
{{所需信息项}}（必填）
要求：
提供每个缺失项的名称（必选）、含义（可选）、格式要求（可选）、样例（可选）、缺失项之间的关系（与、或）（可选）。
样例：
1. 节能区域信息，如松山湖
2. 节能速率保障目标，如20Mbps
3. VLANId，如xxxx(cvlan=11)